
function toast(m){const t=document.getElementById('toast');if(!t)return;t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1700);}
function playAudio(path,fallbackText,lang){const a=new Audio(path);a.play().catch(()=>browserSpeak(fallbackText,lang));}
function browserSpeak(text,lang){if(!('speechSynthesis' in window)){toast('Audio could not play. Use the pronunciation guide shown under the word.');return;}speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.rate=.78;const voices=speechSynthesis.getVoices();const base=(lang||'').split('-')[0].toLowerCase();const v=voices.find(v=>v.lang.toLowerCase()===(lang||'').toLowerCase())||voices.find(v=>v.lang.toLowerCase().startsWith(base));if(v)u.voice=v;if(lang)u.lang=lang;speechSynthesis.speak(u);}
function filterWords(cat,btn){document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('.word').forEach(w=>w.style.display=(cat==='All'||w.dataset.cat===cat)?'block':'none');}
function quiz(btn){const box=btn.closest('.quiz'),ok=btn.dataset.correct==='1';box.querySelectorAll('.q').forEach(x=>x.disabled=true);btn.classList.add(ok?'correct':'wrong');if(!ok){const c=box.querySelector('[data-correct="1"]');if(c)c.classList.add('correct');toast('Good try — the green answer is correct.');}else toast('Correct! ⭐');}
function saveProgress(slug,n){localStorage.setItem('bhashalearn22:'+slug,String(n));showProgress(slug);toast('Progress saved ⭐');}
function showProgress(slug){const n=parseInt(localStorage.getItem('bhashalearn22:'+slug)||'0',10),pct=Math.min(100,n*20);document.querySelectorAll('[data-progress]').forEach(x=>x.style.width=pct+'%');document.querySelectorAll('[data-progress-label]').forEach(x=>x.textContent=pct+'%');}
